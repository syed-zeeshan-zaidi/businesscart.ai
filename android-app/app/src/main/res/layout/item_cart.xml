<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="?android:attr/selectableItemBackground"
    android:minHeight="72dp">

    <ImageView
        android:id="@+id/cartItemImage"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:src="@tools:sample/avatars" />

    <!-- Product Name -->
    <TextView
        android:id="@+id/cartItemName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Product Name"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/cartItemImage"
        app:layout_constraintEnd_toStartOf="@id/cartItemPrice" />

    <!-- Price -->
    <TextView
        android:id="@+id/cartItemPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="$10.00"
        android:textSize="16sp"
        android:textColor="@android:color/darker_gray"
        app:layout_constraintTop_toTopOf="@id/cartItemName"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Discounted Price -->
    <TextView
        android:id="@+id/cartItemDiscountedPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="$8.00"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/cartItemPrice"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Quantity label -->
    <TextView
        android:id="@+id/cartItemQuantityLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Qty:"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginTop="8dp"
        android:layout_marginStart="8dp"
        app:layout_constraintTop_toBottomOf="@id/cartItemName"
        app:layout_constraintStart_toEndOf="@id/cartItemImage" />

    <!-- Clickable quantity badge (flat) -->
    <TextView
        android:id="@+id/cartItemQuantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        tools:text="1"
        android:textSize="16sp"
        android:textColor="#000000"
        android:background="?attr/selectableItemBackground"
        app:layout_constraintStart_toEndOf="@id/cartItemQuantityLabel"
        app:layout_constraintTop_toTopOf="@id/cartItemQuantityLabel"
        app:layout_constraintBottom_toBottomOf="@id/cartItemQuantityLabel"/>

    <!-- Line Item Total -->
    <TextView
        android:id="@+id/lineItemTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="$10.00"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toBottomOf="@id/cartItemQuantityLabel"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Red "Remove" -->
    <TextView
        android:id="@+id/removeCartItemButton"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:gravity="center"
        android:paddingHorizontal="12dp"
        android:text="Remove"
        android:textColor="#FF5252"
        android:textSize="14sp"
        android:background="?attr/selectableItemBackground"
        app:layout_constraintTop_toBottomOf="@id/lineItemTotal"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>